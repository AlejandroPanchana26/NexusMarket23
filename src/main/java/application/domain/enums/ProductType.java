package application.domain.enums;

// Clasificación técnica del producto. Es un enum porque son dos valores fijos
// sin metadatos de negocio (a diferencia de los catálogos de estado).
public enum ProductType {
    PHYSICAL,
    DIGITAL
}
